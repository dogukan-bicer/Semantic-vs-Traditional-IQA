# Semantic-vs-Traditional-IQA - requirements.txt
# Last updated: 2025-09-07

# --- Core scientific / image processing ---
numpy>=1.23
scipy>=1.8
Pillow>=9.0
scikit-image>=0.19

# --- Dataframe / Excel support ---
pandas>=1.5
openpyxl>=3.0

# --- Similarity / quality utilities ---
tqdm>=4.64
matplotlib>=3.5    # optional (plots)
seaborn>=0.12      # optional

# --- Deep learning (PyTorch) ---
# Install the torch build appropriate for your CUDA version (see https://pytorch.org/get-started/locally/)
torch>=2.0.0
torchvision>=0.15.0

# --- Perceptual / metric libraries (optional) ---
lpips>=0.1.4
scikit-learn>=1.1

# --- CLIP / OpenCLIP embeddings (if used) ---
open-clip-torch>=2.0

# --- Dev / testing / formatting ---
pytest>=7.0
black>=23.1

# --- Optional research implementations (install from source if needed) ---
# DINOv2 (Tiny / Large): model code and helpers (weights downloaded separately)
# pip install git+https://github.com/facebookresearch/dinov2.git@main

# DISTS implementation (if not available on PyPI)
# pip install git+https://github.com/dingkeyan93/DISTS-pytorch.git

# Notes:
# - For reproducible GPU support, install torch with the wheel matching your CUDA version.
# - DINOv2 model weights (tiny / large) are typically downloaded from the official repo or Hugging Face; load corresponding checkpoint in code.
